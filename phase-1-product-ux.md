# 墨屿阅读 / Moyu Reader

## Gate 1 — 产品定义与 UX 架构

- 版本：V1.0 产品基线
- 日期：2026-08-21
- 产品形态：Android 原生、本地文件驱动、完全离线的中文小说阅读器
- 核心承诺：导入可靠、阅读舒适、位置稳定、数据留在设备内

---

## 1. 产品定义

### 1.1 一句话定位

为长期阅读中文 TXT 与 DRM-Free EPUB 的用户提供一款安静、稳定、排版优先的 Android 本地小说阅读器。

### 1.2 核心用户任务

1. 从系统文件选择器一次导入一本、多本或一个目录中的小说。
2. 在大型本地书库中快速找到想读的书，并从上次位置继续。
3. 以适合中文小说的排版长时间阅读，随时调整但不丢失语义位置。
4. 在整本书中搜索、跳章、添加书签或标注。
5. 在本地备份书库状态，并可完整恢复。

### 1.3 V1 成功标准

- 首次用户可在 60 秒内完成首次导入并开始阅读。
- 常规用户从启动到继续阅读不超过两次主要点击。
- 修改字号、行距、主题、方向后仍能回到同一语义位置附近。
- 100 MB TXT 的导入、解析、索引不占用主线程且不导致 OOM。
- 飞行模式下所有核心任务保持可用，应用清单不包含 `android.permission.INTERNET`。

### 1.4 V1 边界

**包含**

- TXT：UTF-8、UTF-8 BOM、UTF-16、GBK、GB18030、Big5。
- DRM-Free EPUB 2 / EPUB 3。
- 文件、多文件、授权目录导入。
- 书架、书籍详情、分页/滚动阅读、目录、全文搜索、书签、阅读设置、自定义字体、统计、备份恢复。

**排除**

- 登录、云同步、在线书城、在线搜索、广告、推荐、社区、在线封面与在线字体。
- MOBI、AZW、AZW3、PDF、CBZ、CBR、DRM EPUB。
- 复杂 3D Curl；V1 采用 Instant、Slide、Cover。

---

## 2. 体验原则

1. **正文始终第一**：正文画面不承载非必要装饰与常驻操作。
2. **语义位置优于页码**：以章节与字符偏移保存位置，页码仅是当前排版的派生结果。
3. **系统能力优先**：文件与目录访问统一使用 Storage Access Framework。
4. **过程可见、退出安全**：长任务显示阶段、数量与结果；离开页面后任务状态可恢复。
5. **渐进呈现复杂度**：默认设置足够优秀，高级排版项折叠到二级区域。
6. **错误可行动**：每条错误说明发生了什么，并提供重试、切换编码、重新选择文件或查看详情等明确动作。
7. **克制而有触感**：动效表达空间关系、状态变化和反馈，不与阅读竞争。
8. **隐私即产品能力**：所有文字承诺与工程事实一致。

---

## 3. Information Architecture

```text
应用
├─ 首次引导
│  ├─ 欢迎
│  ├─ 本地与离线说明
│  └─ 导入第一本书
├─ 书架（主目的地）
│  ├─ 最近阅读
│  ├─ 全部书籍
│  ├─ 收藏
│  ├─ 搜索
│  ├─ Grid / List
│  ├─ 排序 / 筛选
│  └─ Selection Mode
├─ 导入
│  ├─ 单文件
│  ├─ 多文件
│  ├─ 授权目录扫描
│  ├─ 发现与去重
│  ├─ 编码检测 / 手动编码
│  ├─ 解析与索引进度
│  └─ 导入结果
├─ 书籍详情
│  ├─ 封面 / 元数据
│  ├─ 阅读状态
│  ├─ 继续阅读
│  ├─ 目录预览
│  └─ 编辑 / 删除 / 重置进度
├─ 阅读器
│  ├─ 正文
│  ├─ 控制层
│  ├─ 目录抽屉
│  ├─ 进度面板
│  ├─ 全书搜索
│  ├─ 书签 / 标注
│  ├─ 阅读设置
│  └─ 更多
└─ 设置
   ├─ 阅读默认值
   ├─ 字体管理
   ├─ 主题与外观
   ├─ 阅读统计
   ├─ 数据管理
   ├─ 备份恢复
   └─ 关于 / 隐私
```

### 3.1 一级导航

- 手机采用三目的地底部导航：**书架 / 导入 / 设置**。
- 阅读器是沉浸式目的地，不显示底部导航；系统返回先关闭当前浮层，再返回详情/书架。
- 书籍详情是书架的层级内页面；搜索结果保持来源上下文。
- 平板扩展为 Navigation Rail + 双栏内容，但信息架构不变。

### 3.2 书架组织模型

- 顶部：标题、搜索、视图切换与更多。
- 最近阅读：仅在存在最近阅读记录时出现，突出“继续阅读”。
- 主列表：全部 / 收藏分段切换；排序与筛选由底部 Sheet 承载。
- 长按进入 Selection Mode；系统返回退出选择，不离开书架。
- 空书架唯一主行动为“导入书籍”，次行动为“选择文件夹”。

---

## 4. 核心流程

### 4.1 首次导入

```text
欢迎
→ 查看离线承诺
→ 导入第一本书
→ 系统文件选择器
→ 复制到 App Private Storage
→ hash 去重
→ 检测格式 / 编码
→ 解析元数据与章节
→ 建立全文索引
→ 导入完成
→ 书籍详情
→ 开始阅读
```

关键规则：

- 复制成功后才进入解析；源文件移动或删除不影响阅读。
- hash 计算、解析、索引全部在后台调度器执行。
- 索引未完成时允许先读已解析章节；搜索入口显示“正在建立索引”。
- 发现重复时默认保留现有副本，并提供“查看已有书籍”与“仍然导入”。

### 4.2 批量 / 文件夹导入

```text
选择文件或目录
→ 扫描受支持文件
→ 列出发现 / 重复 / 不支持
→ 用户确认
→ 并发受控的复制与解析队列
→ 逐项状态
→ 汇总成功、跳过、失败
→ 返回书架
```

批量任务支持后台继续、重试失败项和取消未开始项；取消不回滚已经成功导入的书。

### 4.3 继续阅读与恢复

```text
启动
→ 书架最近阅读
→ 点击书籍
→ 恢复 chapterId + characterOffset
→ 以当前排版重新分页
→ 定位到最接近的页面/滚动位置
```

位置保存触发：翻页/滚动节流、切章、进入后台、退出阅读器。完成持久化前保留最后一次更新。

### 4.4 阅读中查找

```text
打开控制层
→ 全书搜索
→ 输入关键词
→ FTS 异步查询
→ 按章节展示前后文与高亮
→ 点击结果
→ 读取目标章节
→ 定位字符偏移
→ 返回正文并短暂强调命中位置
```

### 4.5 重新识别编码 / 章节

```text
书籍详情 > 更多
→ 重新解析
→ 选择自动检测或指定编码
→ 预览首段文字
→ 选择章节识别策略
→ 后台重建章节与索引
→ 将旧位置映射到新字符偏移
→ 提交或保留旧结果
```

解析结果采用事务切换；失败时旧版本仍可阅读。

### 4.6 本地备份与恢复

```text
设置 > 备份与恢复
→ 选择是否包含小说源文件
→ 选择系统目标位置
→ 创建临时归档并校验
→ 原子写入 MoyuBackup.zip
```

恢复时先验证版本、manifest、校验和与可用空间，再展示合并策略；数据库与文件全部恢复成功后切换。

---

## 5. 阅读器 UX 规格

### 5.1 默认中文排版

- 字体：系统中文正文衬线字体优先，缺失时使用系统可用 fallback。
- 字号：用户可调，默认以 393 × 852 设备在正常字体缩放下约 18sp 为起点。
- 行高：默认约 1.75 倍；段间距与首行缩进协调，避免双重放大段落间隔。
- 页边距：根据 360 / 393 / 412dp 宽度使用区间化 token，不写死像素。
- 标题、正文、页眉页脚分别建模；正文不直接复用 Material UI Body。
- Light、Dark、OLED、Paper 各自校验对比度与长读眩光感。

### 5.2 默认手势

- 中央点击：显示 / 隐藏控制层。
- 左侧点击：上一页；右侧点击：下一页。
- 横滑：分页模式翻页。
- 纵滑：滚动模式滚动；分页模式默认不绑定亮度，用户可在设置中选择亮度或禁用。
- 控制层出现时正文版面不重排，只在其上方叠加。

### 5.3 阅读设置层级

**一级快捷项**：字号、主题、亮度、翻页方式、目录。

**二级排版项**：字重、行距、段距、页边距、对齐、首行缩进、字体。

**行为项**：屏幕方向、常亮、上下滑行为、Reduced Motion。

每次调整即时预览；需要重新分页的设置使用短暂非阻塞进度反馈，并保持锚点。

### 5.4 控制层

- 顶部：返回、书名、更多。
- 底部第一层：上一章、进度滑杆、下一章。
- 底部第二层：目录、搜索、主题、设置。
- 出现/消失采用透明度与小幅位移组合，建议 180–240ms；正文不缩放、不跳动。

### 5.5 阅读位置数据

```text
bookId
chapterId
characterOffset
chapterPercentage
bookPercentage
layoutFingerprint
updatedAt
```

`layoutFingerprint` 只用于判断分页缓存是否可复用，不作为恢复位置的主键。

---

## 6. 页面与状态清单

### 6.1 Onboarding

| 页面 | 必备状态 / 行动 |
|---|---|
| 欢迎 | 品牌、价值主张、开始 |
| 离线说明 | 本地存储、无账户、无上传、继续 |
| 首次导入 | 文件、多文件、文件夹、暂时跳过 |

### 6.2 Library

| 页面 | 必备状态 / 行动 |
|---|---|
| 空书架 | 导入文件、选择文件夹 |
| 有书书架 | 最近阅读、全部/收藏、进度 |
| Grid / List | 无损切换并持久化偏好 |
| Search | 空查询、结果、无结果 |
| Sort / Filter | 当前条件可见、清除 |
| Selection | 删除、已读、重置、编辑 |

### 6.3 Import

| 状态 | 用户可见信息 | 行动 |
|---|---|---|
| 扫描中 | 已发现数量、当前目录 | 取消 |
| 复制中 | 文件名、总体/单项进度 | 后台继续 |
| 解析中 | 编码、章节/元数据阶段 | 后台继续 |
| 索引中 | 已索引章节 | 先阅读 |
| 重复 | 原书信息、文件差异 | 查看已有/仍导入/跳过 |
| 编码不确定 | 置信度与首段预览 | 选择编码 |
| 失败 | 简短原因 | 重试/重新选择/详情 |
| 完成 | 成功、跳过、失败汇总 | 查看书架 |

### 6.4 Reader

| 状态 | 行为 |
|---|---|
| 初次分页 | 优先展示当前锚点附近内容，后台完成相邻页 |
| 章节加载 | 保留旧页，目标内容准备后切换 |
| 章节为空 | 跳过或显示空章节说明，不中断全书 |
| 索引未完成 | 搜索入口显示进行中并允许稍后重试 |
| 源文件损坏 | 使用私有副本；副本也损坏时提供重新导入 |
| 字体失效 | 自动回退默认字体并提示 |

---

## 7. 异常与恢复策略

| 场景 | 用户提示原则 | 恢复动作 |
|---|---|---|
| 文件不可读 / 权限失败 | 说明具体文件 | 重新选择 |
| 空 TXT | 文件没有可阅读文字 | 移除或选择其他文件 |
| 编码错误 | 文字预览异常 | 手动切换编码并重建 |
| 超大 TXT | 明确仍在后台处理 | 后台继续 / 取消 |
| EPUB 损坏 | 指明容器、目录或内容问题的可读分类 | 重试 / 移除 |
| EPUB 无元数据/封面 | 自动使用文件名与本地封面 | 编辑信息 |
| 重复导入 | 展示现有副本 | 查看 / 跳过 / 保留两份 |
| 空间不足 | 展示预计需要空间 | 清理后重试 |
| 备份损坏 | 校验失败且不覆盖现有数据 | 选择其他备份 |
| 数据库迁移失败 | 保留原库备份 | 重试迁移 / 从备份恢复 |

---

## 8. 内容与微文案基线

- 空书架标题：`书架还是空的`
- 空书架说明：`从设备导入 TXT 或 EPUB，阅读内容只保存在本机。`
- 主行动：`导入书籍`
- 索引进行中：`正在建立全文索引，你可以先开始阅读。`
- 离线承诺：`无需账户，不上传小说或阅读数据。`
- 删除确认需区分：仅从书架移除 / 同时删除应用私有副本。
- 技术细节放在“详情”，主提示只提供用户能理解的原因和下一步。

---

## 9. 无障碍与适配

- 交互目标符合 Material 最小触控尺寸。
- 图标按钮具备清晰 `contentDescription`；进度、章节、选中状态具备语义。
- UI 字体缩放后允许标题换行，操作区不遮挡；正文缩放由阅读设置独立管理。
- 颜色不是唯一状态信号；错误、成功、选中均同时使用文字/图标/语义。
- Reduced Motion 下用淡入淡出替代大位移与弹性效果。
- 360 × 800、393 × 852、412 × 915 均作为手机验收视口；平板采用响应式双栏而非等比拉伸。

---

## 10. 设计阶段输入

### 10.1 必须在四套风格稿中共同验证

- 书架内容密度与“继续阅读”优先级。
- 中文书名、作者、进度的层级。
- Reader 正文字号、行高、页边距、页眉页脚的舒适度。
- 控制层覆盖正文时的层次与系统栏处理。
- Light 到 Dark / OLED 的可延展性。
- Android 手势区、状态栏与原生导航感。

### 10.2 四套稿的固定比较维度

1. 阅读舒适度。
2. 品牌辨识度。
3. 信息层级与操作效率。
4. Light / Dark / OLED / Paper 扩展性。
5. Jetpack Compose 与 Material 3 的实现复杂度。
6. 长期使用的视觉耐久性。

---

## Gate 1 结论

- 产品闭环成立：导入 → 组织 → 阅读 → 查找/标记 → 持久化 → 备份恢复。
- 三个一级导航目的地足以覆盖 V1，阅读器保持独立沉浸上下文。
- 位置、解析与导入流程均定义了稳定恢复策略。
- Gate 2 可据此制作四套视觉方向；本阶段不进入 Figma 或 Android 实现。
